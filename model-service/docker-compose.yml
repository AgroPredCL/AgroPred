version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./modelos:/model-service/modelos
    environment:
      - PYTHONUNBUFFERED=1